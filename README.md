# UrCrochetHub
React + Vite version of the original Java Swing app, ready for Vercel.

## Run locally
npm install
npm run dev

## Deploy
Push this folder to GitHub, then import the repository in Vercel. Build command: `npm run build`; output directory: `dist`.
